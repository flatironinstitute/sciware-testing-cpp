#include <stdexcept>
#include <vector>
#include <string>

// only needed for solution 2
#include <algorithm>
#include <functional>

std::vector<double> add_vectors(std::vector<double> &v1, std::vector<double> &v2);
